version: "2"

sql:
  - engine: postgresql
    schema: sql/schema
    queries: sql/queries
    gen:
      go:
        package: db
        out: internal/todo/db
        sql_package: pgx/v5
        emit_json_tags: true